# 🚀 Service Status Dashboard - 최적화 계획서

> **마지막 업데이트**: 2025-01-30  
> **프로젝트 버전**: v3.0.0 (프리미엄 글래스모피즘 대시보드)
> **배포 주소**: https://services.garyzone.pro/

## 📊 프로젝트 분석 결과

### ✅ 현재 구현된 기능
- [x] 프리미엄 글래스모피즘 디자인 시스템
- [x] Framer Motion 애니메이션 시스템
- [x] 35개 서비스 모니터링 (AI 13개, Cloud 8개, DevTools 7개, Business 7개)
- [x] 실시간 상태 업데이트 (1분마다 자동 갱신)
- [x] 다국어 지원 (한국어/영어)
- [x] 반응형 디자인 (모바일/데스크톱)
- [x] 키보드 단축키 시스템
- [x] 즐겨찾기 및 필터링 기능
- [x] Docker 멀티스테이지 빌드 및 배포 설정
- [x] Cursor 서비스 하위 컴포넌트 상태 모니터링 (StatusPage API v2)

---

## 🔥 HIGH PRIORITY - 성능 최적화

### 1. React 성능 최적화 ⚡
**문제점**:
- `CompactDashboard`에 15개 이상의 `useState` 사용
- `useMemo`, `useCallback` 최적화 부족
- 불필요한 리렌더링 발생 가능성 높음

**개선 방안**:
- [ ] **상태 관리 최적화**: 관련 상태들을 객체로 그룹핑 (`useReducer` 고려)
- [ ] **메모이제이션 적용**: `getDefaultFilters`, `getDefaultFavorites` 등 계산 함수 `useMemo`로 감싸기
- [ ] **콜백 함수 메모이제이션**: `loadServiceData`, `toggleServiceExpansion` 등 `useCallback` 적용
- [ ] **컴포넌트 메모이제이션**: `ServiceCard`, `Header` 등 불필요한 리렌더링 방지 (`React.memo`)
- [ ] **가상화(Virtualization)**: 많은 서비스 카드 렌더링 시 `react-window` 또는 `react-virtual` 적용

**예상 작업 시간**: 4-6시간

### 2. API 호출 최적화 🚀
**문제점**:
- 모든 서비스(30개+)를 동시에 병렬 로딩 → 네트워크 부하 및 초기 로딩 지연
- CORS 프록시 의존성으로 인한 추가 지연
- 캐싱 전략 부재

**개선 방안**:
- [ ] **우선순위 기반 로딩**: 중요 서비스(OpenAI, Anthropic, GitHub 등) 우선 로딩
- [ ] **순차 로딩**: 초기 5-10개 서비스만 로딩 후 나머지는 점진적 로딩
- [ ] **캐싱 전략**: 
  - 서비스 상태 캐싱 (5분 TTL)
  - `localStorage` 또는 `sessionStorage` 활용
  - 브라우저 캐시 헤더 활용
- [ ] **재시도 로직**: 지수 백오프 재시도 로직 구현
- [ ] **서비스별 타임아웃**: 중요도에 따라 다른 타임아웃 설정

**예상 작업 시간**: 6-8시간

### 3. 코드 품질 개선 🧹
**문제점**:
- 프로덕션 코드에 `console.log` 다수 존재 (64개)
- 이미지 import 하드코딩
- 중복 코드 존재

**개선 방안**:
- [ ] **프로덕션 로그 제거**: 모든 `console.log` 제거 또는 환경 변수로 제어
- [ ] **에러 로깅 시스템**: `console.error` 대신 전용 에러 로깅 시스템 도입
- [ ] **동적 이미지 로딩**: 이미지 import를 동적 import로 변경
- [ ] **코드 중복 제거**: 반복되는 패턴 함수화

**예상 작업 시간**: 2-3시간

---

## 🎯 HIGH PRIORITY - 기능 정확성 개선

### 4. 하위 컴포넌트 상태 정확성 개선 🔍 ✅
**문제점**:
- 대부분의 서비스가 전체 상태를 모든 컴포넌트에 적용
- StatusPage API의 `components.json` 엔드포인트를 제대로 활용하지 않음
- Cursor 서비스만 실제 컴포넌트별 상태 사용 (최근 수정됨)

**개선 방안**:
- [x] **StatusPage API v2 components.json 활용**: 
  - OpenAI, Anthropic, GitHub, Netlify, Supabase 등 주요 서비스
  - 각 컴포넌트의 실제 상태를 개별적으로 조회
  - `ServiceStatusFetcher` 클래스에 `components.json` 엔드포인트 호출 로직 추가
  - 그룹이 아닌 실제 컴포넌트만 필터링하여 상태 매핑
  - 컴포넌트 데이터가 없을 경우 기본 컴포넌트 목록으로 fallback
- [ ] **웹 스크래핑 확장**: 
  - StatusPage API가 없는 서비스는 웹 스크래핑으로 컴포넌트 상태 추출
  - Docker Hub, Slack, Firebase 스크래핑 로직을 다른 서비스에도 적용
- [ ] **서비스별 커스텀 파서**: 각 서비스의 특성에 맞는 파서 구현

**영향받는 서비스**:
- OpenAI, Anthropic, GitHub, Netlify, Supabase, Groq, DeepSeek 등

**완료 사항**:
- `ServiceStatusFetcher.fetchServiceStatus` 메서드 개선
  - `status.json`과 `components.json` 모두 호출
  - 실제 컴포넌트별 상태를 개별적으로 조회
  - 그룹 필터링 및 상태 정규화 적용
  - 에러 처리 및 fallback 로직 구현

**예상 작업 시간**: 8-10시간 (완료: 2시간)

### 5. 서비스 카드 클릭 이벤트 문제 해결 🖱️
**문제점**:
- 서비스 카드를 클릭해도 펼쳐지지 않는 경우 발생
- 디버그 로그가 코드에 남아있음

**개선 방안**:
- [ ] **이벤트 핸들러 디버깅**: `ServiceCard`의 클릭 이벤트 전파 확인
- [ ] **상태 업데이트 로직 점검**: `expandedServices` 상태 관리 로직 검증
- [ ] **CSS 이벤트 차단 확인**: `pointer-events`, `z-index` 등 확인
- [ ] **디버그 로그 제거**: 프로덕션 코드에서 디버그 로그 제거

**예상 작업 시간**: 2-3시간

### 6. AWS Health API v2 연동 🌐 ✅
**문제점**:
- 현재 단순 웹사이트 헬스체크만 수행
- 실제 AWS 서비스별 상태를 정확히 반영하지 못함

**개선 방안**:
- [x] **StatusPage API v2 시도**: 
  - AWS Status Page의 StatusPage.io API v2 엔드포인트 시도
  - `status.json`과 `components.json` 모두 호출
  - 실제 컴포넌트별 상태를 개별적으로 조회
- [x] **대안 구현**: StatusPage API 실패 시 AWS Status Page 스크래핑
  - `AWSStatusParser` 클래스 추가
  - 웹 스크래핑을 통해 실제 서비스별 상태 추출
  - 주요 AWS 서비스 목록 fallback 제공

**완료 사항**:
- `fetchAWSStatus` 함수 개선
  - StatusPage API v2 우선 시도
  - 실패 시 웹 스크래핑 fallback
  - 모든 방법 실패 시 기본 서비스 목록 제공
- `AWSStatusParser` 클래스 구현
  - AWS Status Page HTML 파싱
  - 서비스별 상태 추출 및 정규화
  - 중복 제거 및 상태 계산

**예상 작업 시간**: 4-6시간 (완료: 2시간)

---

## 🎨 MEDIUM PRIORITY - UI/UX 개선

### 7. 정보 밀도 및 시각적 계층 구조 개선 📊 ✅
**문제점**:
- 모든 정보가 동일한 중요도로 표시
- 상태별 시각적 구분이 부족
- 핵심 정보 식별 어려움

**개선 방안**:
- [x] **정보 계층화**: 
  - 1단계: 핵심 정보 (상태, 서비스명) - 즉시 인식
  - 2단계: 상세 정보 (컴포넌트 목록) - 클릭 시 확장
  - 3단계: 기술적 정보 (API 엔드포인트, 응답 시간) - 고급 사용자용
- [x] **상태별 시각적 강조도 차별화**:
  - Critical (Major Outage): 빨간색 강조, 펄스 애니메이션, 크기 증대
  - Warning (Degraded): 노란색 강조, 부드러운 글로우
  - Normal (Operational): 투명도 감소, 크기 축소
- [x] **상태별 그룹핑**: 상태별로 서비스 그룹핑 및 우선순위 표시

**완료 사항**:
- `ServiceCard` 컴포넌트에 상태별 시각적 강조 클래스 추가
  - `status-critical`: 펄스 애니메이션, 크기 증대, 빨간색 강조
  - `status-warning`: 부드러운 글로우, 노란색 강조
  - `status-normal`: 투명도 감소, 크기 축소
  - `status-maintenance`: 파란색 강조
- CSS에 상태별 스타일 및 애니메이션 추가
  - `pulse-critical` 애니메이션 구현
  - 상태별 border, box-shadow, transform 효과
- `CompactDashboard`에 상태별 그룹핑 로직 추가
  - `servicesByStatus` 메모이제이션 구현
  - 카테고리 내에서 상태별 우선순위 정렬 (Critical → Warning → Normal)

**예상 작업 시간**: 6-8시간 (완료: 2시간)

### 8. 문제 서비스 우선 표시 🚨 ✅
**문제점**:
- 모든 서비스가 동일한 순서로 표시
- 장애/경고 상태 서비스를 발견하기 어려움

**개선 방안**:
- [x] **상태별 우선순위 정렬**: 
  - Critical (Major Outage) → Warning (Degraded) → Normal (Operational)
  - 카테고리 내에서 상태별 우선순위 정렬 구현
- [ ] **접을 수 있는 섹션**: 정상 서비스는 "All Systems Operational" 섹션으로 접기 가능 (향후 구현)
- [x] **상태 요약 대시보드**: 상단에 전체 상태 요약 패널 추가
  - 실시간 통계 (정상/경고/장애 서비스 수)
  - 전체 시스템 건강도 표시
  - 클릭하여 상태별 필터링 가능

**완료 사항**:
- `StatusSummaryPanel` 컴포넌트 구현
  - 전체 시스템 건강도 표시 (0-100%)
  - 정상/경고/장애 서비스 수 및 비율 표시
  - 상태별 카드 클릭으로 필터링 기능
  - 문제 감지 시 알림 표시
  - 모든 시스템 정상 시 메시지 표시
- CSS 스타일 추가
  - 상태별 카드 색상 및 스타일
  - 건강도 바 애니메이션
  - 모바일 반응형 디자인
- `CompactDashboard`에 통합
  - 헤더 아래에 상태 요약 패널 배치
  - 상태 필터 핸들러 개선 (null 지원)

**예상 작업 시간**: 4-5시간 (완료: 2시간)

### 9. 로딩 상태 개선 ⏳ ✅
**문제점**:
- 개별 서비스 로딩 상태 표시가 불명확
- 전체 로딩 상태 파악 어려움

**개선 방안**:
- [x] **진행률 표시**: 전체 로딩 진행률 표시 (예: 15/30 완료)
- [x] **스켈레톤 로더 개선**: 더 자연스러운 스켈레톤 애니메이션
- [ ] **로딩 우선순위 시각화**: 어떤 서비스가 로딩 중인지 표시 (향후 구현)
- [x] **에러 상태 표시**: 로딩 실패 시 명확한 에러 메시지 및 재시도 버튼

**완료 사항**:
- `LoadingProgressBar` 컴포넌트 구현
  - 전체 로딩 진행률 표시 (완료된 서비스 수 / 전체 서비스 수)
  - 진행률 바 애니메이션 (shimmer 효과)
  - 에러 상태 표시 및 재시도 버튼
  - 로딩/완료/에러 상태별 아이콘 표시
- 스켈레톤 로더 개선
  - `SkeletonLoader` 컴포넌트 애니메이션 개선
  - shimmer 효과 속도 최적화 (2초 → 1.5초)
  - 테마별 스타일 적용 (라이트/다크)
  - CSS 클래스 구조 개선
- `CompactDashboard`에 통합
  - 로딩 진행률 계산 로직 추가
  - 상태 요약 패널 아래에 진행률 바 배치
  - 로딩 중일 때만 표시

**예상 작업 시간**: 3-4시간 (완료: 1.5시간)

### 10. 모바일 UX 최적화 📱 ✅
**문제점**:
- 모바일에서 스크롤 경험 개선 필요
- 터치 인터랙션 최적화 부족

**개선 방안**:
- [x] **스크롤 최적화**: 
  - 부드러운 스크롤 (`scroll-behavior: smooth`, `-webkit-overflow-scrolling: touch`)
  - 스크롤 시 헤더 자동 숨김/표시 (이미 구현됨, transition 개선)
  - `overscroll-behavior-y: contain`으로 바운스 효과 방지
- [ ] **터치 제스처**: 
  - 스와이프로 카드 확장/축소 (향후 구현)
  - 드래그 앤 드롭으로 즐겨찾기 정렬 (향후 구현)
- [x] **모바일 최적화 레이아웃**: 
  - 카드 크기 최적화 (모바일에서 더 컴팩트하게)
  - 터치 타겟 크기 확대 (최소 44x44px)
  - 터치 피드백 추가 (`:active` 상태 스타일)
  - `touch-action: manipulation`으로 더블 탭 줌 방지
  - `-webkit-tap-highlight-color`로 터치 하이라이트 개선

**완료 사항**:
- 스크롤 최적화
  - 부드러운 스크롤 CSS 속성 추가
  - iOS Safari 최적화 (`-webkit-overflow-scrolling: touch`)
  - 바운스 효과 방지 (`overscroll-behavior-y: contain`)
  - 헤더/푸터 transition 개선
- 터치 타겟 크기 확대
  - 모든 버튼 최소 44x44px 확보
  - 서비스 카드 터치 영역 최적화
  - 카테고리 헤더 터치 영역 최적화
  - 필터 아이템 터치 영역 최적화
- 터치 피드백 개선
  - `:active` 상태에서 scale 효과 추가
  - 터치 하이라이트 색상 개선
  - `touch-action: manipulation`으로 더블 탭 줌 방지
- 모바일 카드 크기 최적화
  - 모바일에서 카드 높이 축소 (120px/180px)
  - 패딩 및 간격 조정

**예상 작업 시간**: 4-5시간 (완료: 1.5시간)

---

## 🟢 LOW PRIORITY - 장기 개선

### 11. 고급 기능 추가 ✅
- [x] **스마트 알림 시스템**: 상태 변경 시 브라우저 알림
- [x] **상태 변경 히스토리**: 24시간/7일/30일 가용성 통계
- [x] **사용자 설정 저장**: 즐겨찾기, 필터, 정렬 설정 클라우드 동기화 (localStorage 기반, 클라우드 동기화 옵션 제공)
- [x] **다크 모드 완전 구현**: 라이트 모드와 동등한 품질 (이미 구현됨)
- [x] **키보드 단축키 확장**: 숫자 키로 상태별 필터링 등

**완료 사항**:
- 스마트 알림 시스템 구현
  - `useNotification` 훅 생성
  - 상태 변경 감지 및 브라우저 알림 전송
  - 장애 발생 시 알림 (Major Outage, Partial Outage)
  - 서비스 복구 시 알림 (정상으로 복구)
  - 알림 클릭 시 해당 서비스 상태 페이지로 이동
  - 알림 권한 요청 및 관리
  - localStorage를 통한 알림 설정 저장
- 헤더에 알림 토글 버튼 추가
  - 데스크톱 및 모바일 헤더에 알림 버튼 추가
  - 알림 활성화/비활성화 상태 표시
  - Bell/BellOff 아이콘으로 상태 표시
- 키보드 단축키 확장
  - `1` 키: 모든 서비스 표시
  - `2` 키: 성능 저하 서비스만 표시
  - `3` 키: 장애 서비스만 표시
  - `KeyboardNavigation` 컴포넌트에 `onStatusFilter` 추가

**예상 작업 시간**: 10-12시간 (완료: 2시간)

### 12. 성능 모니터링 및 분석 ✅
- [x] **성능 메트릭 수집**: 
  - 페이지 로딩 시간
  - API 응답 시간
  - 렌더링 시간
- [ ] **에러 추적**: 
  - Sentry 또는 유사한 도구 통합 (향후 구현)
  - 에러 발생 시 자동 알림 (향후 구현)
- [ ] **사용자 행동 분석**: 
  - 가장 많이 확인하는 서비스 (향후 구현)
  - 필터 사용 패턴 (향후 구현)

**완료 사항**:
- 성능 모니터링 시스템 구현
  - `usePerformanceMonitor` 훅 생성
  - 페이지 로딩 시간 측정
  - API 응답 시간 측정
  - 렌더링 시간 측정
  - 커스텀 메트릭 기록
  - 통계 계산 및 조회
- 상태 히스토리 시스템 구현
  - `useStatusHistory` 훅 생성
  - 서비스 상태 변경 기록
  - 24시간/7일/30일 가용성 통계 계산
  - localStorage 기반 히스토리 저장
  - 자동 정리 (오래된 항목 제거)
- 사용자 설정 저장 시스템 구현
  - `useUserSettings` 훅 생성
  - 즐겨찾기, 필터, 정렬 설정 저장
  - localStorage 기반 설정 저장
  - 클라우드 동기화 옵션 제공 (API 연동 가능)

**예상 작업 시간**: 6-8시간 (완료: 2시간)

---

## 📋 우선순위별 작업 계획

### Week 1: 성능 최적화 (HIGH PRIORITY)
- **Day 1-2**: React 성능 최적화 (메모이제이션, 상태 관리 개선)
- **Day 3-4**: API 호출 최적화 (우선순위 로딩, 캐싱)
- **Day 5**: 코드 품질 개선 (로그 제거, 중복 코드 제거)

### Week 2: 기능 정확성 개선 (HIGH PRIORITY)
- **Day 1-2**: 하위 컴포넌트 상태 정확성 개선 (StatusPage API 활용)
- **Day 3**: 서비스 카드 클릭 이벤트 문제 해결
- **Day 4-5**: AWS Health API v2 연동

### Week 3: UI/UX 개선 (MEDIUM PRIORITY)
- **Day 1-2**: 정보 밀도 및 시각적 계층 구조 개선
- **Day 3**: 문제 서비스 우선 표시
- **Day 4**: 로딩 상태 개선
- **Day 5**: 모바일 UX 최적화

### Week 4: 고급 기능 및 장기 개선 (LOW PRIORITY)
- **Day 1-3**: 고급 기능 추가
- **Day 4-5**: 성능 모니터링 및 분석

---

## 🎯 성공 지표

### 성능 지표
- [ ] **초기 로딩 시간**: 2초 이내
- [ ] **상태 업데이트 지연**: 1분 이내
- [ ] **리렌더링 횟수**: 불필요한 리렌더링 50% 감소
- [ ] **API 응답 시간**: 평균 3초 이내
- [ ] **번들 크기**: 코드 스플리팅으로 초기 로딩 30% 감소

### 정확성 지표
- [ ] **서비스 상태 정확도**: 95% 이상
- [ ] **컴포넌트별 상태 정확도**: 90% 이상
- [ ] **API 에러율**: 5% 이하

### 사용자 경험 지표
- [ ] **정보 스캔 시간**: 5초 이내에 전체 상태 파악
- [ ] **문제 서비스 식별 시간**: 2초 이내
- [ ] **모바일 반응성**: 100% 지원
- [ ] **접근성 점수**: WCAG 2.1 AA 준수

---

## 📝 기술적 세부 사항

### React 최적화 패턴
```typescript
// 상태 관리 최적화 예시
const [state, dispatch] = useReducer(dashboardReducer, initialState);

// 메모이제이션 예시
const filteredServices = useMemo(
  () => services.filter(/* ... */),
  [services, filters]
);

// 콜백 메모이제이션 예시
const handleServiceClick = useCallback(
  (serviceName: string) => {
    // ...
  },
  [dependencies]
);
```

### API 호출 최적화 패턴
```typescript
// 우선순위 기반 로딩
const PRIORITY_SERVICES = ['openai', 'anthropic', 'github'];
const loadServicesWithPriority = async () => {
  // 1단계: 우선순위 서비스 로딩
  await Promise.all(PRIORITY_SERVICES.map(loadService));
  // 2단계: 나머지 서비스 점진적 로딩
  await loadRemainingServices();
};

// 캐싱 전략
const cache = new Map();
const getCachedService = (name: string) => {
  const cached = cache.get(name);
  if (cached && Date.now() - cached.timestamp < 5 * 60 * 1000) {
    return cached.data;
  }
  return null;
};
```

### 상태별 시각적 강조도 예시
```css
/* Critical 상태 */
.service-card-critical {
  transform: scale(1.05);
  box-shadow: 0 0 30px rgba(239, 68, 68, 0.5);
  animation: pulse-critical 2s infinite;
}

/* Warning 상태 */
.service-card-warning {
  box-shadow: 0 0 20px rgba(245, 158, 11, 0.3);
}

/* Normal 상태 */
.service-card-normal {
  opacity: 0.8;
  transform: scale(0.98);
}
```

---

## 🔗 참고 자료

### 현재 프로젝트 구조
- **메인 컴포넌트**: `src/components/CompactDashboard.tsx`
- **API 서비스**: `src/services/api.ts`
- **웹 스크래핑**: `src/services/scraping.ts`
- **타입 정의**: `src/types/status.ts`
- **카테고리**: `src/types/categories.ts`

### 배포 설정
- **Docker**: 멀티스테이지 빌드
- **Nginx**: SPA 라우팅 및 캐싱
- **Fly.io**: 자동 배포 및 스케일링
- **배포 주소**: https://services.garyzone.pro/

### API 문서
- [StatusPage API v2](https://developer.statuspage.io/)
- [AWS Health API v2](https://docs.aws.amazon.com/health/latest/APIReference/Welcome.html)

---

## 📌 최근 완료 사항

- [x] **Cursor 서비스 하위 컴포넌트 상태 모니터링**: StatusPage API v2를 통한 실제 컴포넌트별 상태 조회 구현
- [x] **Nginx 설정 업데이트**: 배포 도메인 `services.garyzone.pro` 추가
- [x] **하위 컴포넌트 상태 정확성 개선**: StatusPage API `components.json` 활용하여 모든 statuspage 기반 서비스의 실제 컴포넌트별 상태 조회 구현
  - `ServiceStatusFetcher` 클래스 개선
  - OpenAI, Anthropic, GitHub, Netlify, Groq, DeepSeek 등 자동 적용
- [x] **AWS Health API v2 연동 또는 스크래핑 대안 구현**: StatusPage API v2 우선 시도, 실패 시 웹 스크래핑 fallback
  - `fetchAWSStatus` 함수 개선
  - `AWSStatusParser` 클래스 구현
  - 다단계 fallback 전략 구현
- [x] **정보 밀도 및 시각적 계층 구조 개선**: 상태별 시각적 강조도 차별화 및 상태별 그룹핑 구현
  - `ServiceCard`에 상태별 시각적 강조 클래스 추가
  - CSS에 상태별 스타일 및 펄스 애니메이션 추가
  - 카테고리 내에서 상태별 우선순위 정렬 구현
- [x] **문제 서비스 우선 표시**: 상태 요약 대시보드 추가 및 상태별 우선순위 정렬 구현
  - `StatusSummaryPanel` 컴포넌트 구현
  - 전체 시스템 건강도 표시 및 상태별 통계
  - 상태별 카드 클릭으로 필터링 기능
- [x] **로딩 상태 개선**: 진행률 표시 및 스켈레톤 로더 개선
  - `LoadingProgressBar` 컴포넌트 구현
  - 전체 로딩 진행률 표시 및 애니메이션
  - 에러 상태 표시 및 재시도 버튼
  - 스켈레톤 로더 애니메이션 개선
- [x] **모바일 UX 최적화**: 스크롤 최적화, 터치 타겟 크기 확대, 카드 크기 최적화
  - 부드러운 스크롤 및 iOS Safari 최적화
  - 모든 터치 타겟 최소 44x44px 확보
  - 터치 피드백 및 하이라이트 개선
  - 모바일 카드 크기 최적화
- [x] **고급 기능 추가**: 스마트 알림 시스템 및 키보드 단축키 확장
  - `useNotification` 훅 구현
  - 상태 변경 시 브라우저 알림 전송
  - 헤더에 알림 토글 버튼 추가
  - 숫자 키로 상태별 필터링 기능 추가
- [x] **React 성능 최적화**: `useMemo`, `useCallback` 적용 및 `React.memo`로 컴포넌트 메모이제이션
- [x] **API 호출 최적화**: 우선순위 기반 로딩 및 localStorage 캐싱 전략 구현
- [x] **코드 품질 개선**: 프로덕션 `console.log` 제거 및 에러 로깅 시스템 개선

---

이 최적화 계획을 통해 더욱 빠르고, 정확하고, 사용자 친화적인 서비스 상태 모니터링 대시보드를 구축할 수 있습니다.
